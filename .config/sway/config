# Colors
set $bg "#1a1b26"
set $bg_dark "#16161e"
set $fg "#c0caf5"
set $fg_dark "#a9b1d6"
set $fg_gutter "#3b4261"
set $blue0 "#3d59a1"
set $blue "#7aa2f7"
set $blue7 "#394b70"
set $red "#db4b4b"

### Output configuration
exec ~/.azotebg
output DP-3 resolution 1920x1080 position 1920,0
output HDMI-A-5 resolution 1920x1080 position 0,0

exec swayidle -w \\
	timeout 300 "swaylock -f" \\
    timeout 900 "systemctl suspend" \\
	before-sleep "swaylock -f"

### Bar
bar {
    status_command i3status-rs ~/.config/sway/status.toml
    position top
    font pango: IntelOneMono, FontAwesome6Free 10
    separator_symbol "|"

    colors {
        background $bg_dark
        statusline $fg_dark
        separator $fg_gutter
        # border, bg, text
        focused_workspace	$bg	$blue0  $fg
        active_workspace	$bg $blue7  $fg
        inactive_workspace	$bg $bg     $fg
        urgent_workspace	$bg $red    $fg
    }
}


### Theming:
default_border pixel 1
default_floating_border normal
hide_edge_borders smart
font pango: Noto Sans Mono 10

# Colors                border  bg          text  indi  childborder
client.focused          $bg     $blue0      $fg   $fg   $blue0
client.focused_inactive $bg     $blue7      $fg   $bg   $bg
client.unfocused        $bg     $bg_dark    $fg   $bg   $bg
client.urgent           $bg     $red        $fg   $red  $red

set $gnome-schema org.gnome.desktop.interface
exec {
    gsettings set $gnome-schema gtk-theme 'tokyonight'
    gsettings set $gnome-schema font-name 'Noto Sans Mono'
}

exec mako
include config.d/*

